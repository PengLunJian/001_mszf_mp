<template>
  <div class="container">
    <div class="content">
      <div class="header">
        <div class="inline">
          <div class="title">LOGO</div>
          <div class="search">
            <div class="btn btn-search">
              <div class="btn-icon iconfont icon-search"></div>
              <div class="btn-text">请输入小区/商圈</div>
            </div>
          </div>
        </div>
      </div>
      <div class="body">
        <div class="row">
          <div class="col-12">
            <navigator class="user" hover-class="none" :url="url">
              <img class="user-bg" src="../../assets/images/mine_bg.jpg"/>
              <div class="user-box">
                <img class="user-img" src="../../assets/images/agent.jpg" mode="aspectFit"/>
                <span class="user-name">WEB前端架构师</span>
              </div>
            </navigator>
          </div>
        </div>
        <customer-navigator></customer-navigator>
      </div>
      <div class="footer"></div>
    </div>
  </div>
</template>

<script type="text/ecmascript-6">
  import wepy from '@wepy/core';
  import store from '../../store/index';
  import * as $routes from '../../router';

  wepy.page({
    store,

    mixins: [],

    data: {
      url: $routes.MINE.path
    },

    computed: {},

    methods: {},

    created() {
    }
  });
</script>

<style scoped lang="less">
  @import "../../assets/less/variable";

  @btnSearchHeight: unit(60, rpx);

  .container {
    .content {
      height: 100vh;
      padding-top: unit(130, rpx);
      .header {
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
        padding-top: unit(40, rpx);
        background-color: @white;
        .inline {
          position: relative;
          .title {
            position: absolute;
            top: 0;
            left: 0;
            width: auto;
            height: unit(90, rpx);
            line-height: unit(90, rpx);
            padding: 0 unit(30, rpx);
            font-size: unit(32, rpx);
            text-align: center;
            font-weight: bold;
          }
          .search {
            height: unit(90, rpx);
            padding: unit(15, rpx) 0;
            overflow: hidden;
            text-align: center;
            font-size: 0;
            .btn-search {
              width: unit(300, rpx);
              height: @btnSearchHeight;
              line-height: @btnSearchHeight;
              display: inline-block;
              overflow: hidden;
              border-radius: @btnSearchHeight;
              background-color: #f0f0f0;
              color: @fontColor3;
              .btn-icon {
                position: absolute;
                top: 0;
                left: 0;
                width: @btnSearchHeight;
                height: @btnSearchHeight;
                font-size: @fontSize3;
              }
              .btn-text {
                height: @btnSearchHeight;
                line-height: @btnSearchHeight;
                padding-left: @btnSearchHeight;
                font-size: @fontSize1;
                text-align: left;
              }
            }
          }
        }
      }
      .body {
        .row {
          padding: unit(15, rpx);
          padding-bottom: 0;
          .col-12 {
            padding: unit(15, rpx);
            .user {
              position: relative;
              height: unit(200, rpx);
              line-height: unit(200, rpx);
              border-radius: unit(20, rpx);
              overflow: hidden;
              font-size: 0;
              .user-bg {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                border-radius: @borderRadius2;
              }
              .user-box {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                padding: 0 unit(30, rpx);
                .user-img {
                  width: unit(80, rpx);
                  height: unit(80, rpx);
                  display: inline-block;
                  vertical-align: middle;
                  border: 1px solid @white;
                  background-color: @white;
                  border-radius: @borderRadius3;
                }
                .user-name {
                  display: inline-block;
                  vertical-align: middle;
                  padding-left: unit(20, rpx);
                  font-size: @fontSize5;
                  letter-spacing: 1px;
                  color: @white;
                }
              }
            }
          }
        }
      }
      .footer {
      }
    }
  }
</style>
<config>
  {
  navigationStyle:'custom',
  usingComponents: {
  'customer-navigator': '../../components/customer-navigator/customer-navigator',
  }
  }
</config>
